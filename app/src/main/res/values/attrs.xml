<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="EditTextLabeledView">
        <attr name="label" format="reference" />
    </declare-styleable>

    <declare-styleable name="ScrollNumberView">
        <attr name="defaultNumber" format="integer" />
        <attr name="minNumber" format="integer" />
        <attr name="maxNumber" format="integer" />
        <attr name="arrowUp" format="reference" />
        <attr name="arrowDown" format="reference" />
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
</resources>